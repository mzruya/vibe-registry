[package]
name = "work"
version = "1.0.0"
description = "Git worktree manager with GitHub PR status, CI check counts, and interactive fuzzy switching"
license = "MIT"
binaries = ["work.sh", "work.nu"]
